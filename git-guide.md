برای درک گیت، همیشه این ۴ محیط را در ذهن داشته باش:
Working Directory: پوشه‌ای که کدهای عادی‌ات در آن است.
Staging Area (Index): صفِ انتظار برای کامیت شدن (git add).
Local Repo: مخزن محلی روی سیستم خودت (git commit).
Remote Repo: مخزن آنلاین مثل گیت‌هاب یا گیت‌لب (git push).

۱. پیکربندی و شروع کار (Setup & Init)
قبل از هر کاری باید هویت خود را به گیت معرفی کنی تا معلوم شود چه کسی کامیت می‌زند.
git init,ساخت یک مخزن (Repository) جدید و محلی در پوشه فعلی
"git config --global user.name ""Your Name""",تنظیم نام شما برای تمام پروژه‌ها
"git config --global user.email ""mail@example.com""",تنظیم ایمیل شما برای تمام پروژه‌ها
git clone <url>,دانلود کامل یک پروژه از اینترنت به همراه تمام تاریخچه آن

۲. مدیریت تغییرات و ثبت (Working with Changes)
دستوراتی که در طول روز بارها و بارها هنگام کدنویسی با آن‌ها سر و کار داری.
git init,ساخت یک مخزن (Repository) جدید و محلی در پوشه فعلی
"git config --global user.name ""Your Name""",تنظیم نام شما برای تمام پروژه‌ها
"git config --global user.email ""mail@example.com""",تنظیم ایمیل شما برای تمام پروژه‌ها
git clone <url>,دانلود کامل یک پروژه از اینترنت به همراه تمام تاریخچه آن

۳. شاخه‌سازی و ادغام (Branching & Merging)
برانچ‌ها به تو اجازه می‌دهند بدون خراب کردن کدهای اصلی پروژه، ویژگی‌های جدید (مثل بخش نقشه یا دیتابیس پایتون) را جداگانه توسعه دهی.
git branch: نمایش لیست تمام شاخه‌های محلی.
git branch <branch-name>: ساخت یک شاخه جدید.
git checkout <branch-name> یا git switch <branch-name>: رفتن به شاخه مورد نظر.
git checkout -b <branch-name>: ساخت یک شاخه جدید و رفتن فوری به داخل آن (ترکیب دو دستور بالا).
git merge <branch-name>: ادغام کدهای شاخه مشخص‌شده در شاخه‌ای که در حال حاضر در آن هستید.
git branch -d <branch-name>: حذف یک شاخه (در صورت ادغام شدن).

۴. همگام‌سازی با سرور (Sharing & Syncing)
دستوراتی که برای تبادل کد بین سیستم خودت و سرورهای ابری (مثل گیت‌هاب) استفاده می‌کنی.
git stash,ذخیره موقت و پنهان کردن تغییرات فعلی بدون کامیت کردن (برای زمانی که می‌خواهید سریع برانچ عوض کنید)
git stash pop,بازگرداندن آخرین تغییراتی که به صورت موقت با stash ذخیره کرده بودید
git log --oneline,نمایش تاریخچه کامیت‌ها به صورت خلاصه، زیبا و در یک خط
git cherry-pick <commit-id>,گلچین کردن؛ آوردن یک کامیت خاص از یک برانچ دیگر به برانچ فعلی شما
git rebase -i HEAD~N,شروع عملیات ریبیس تعاملی روی N کامیت آخر برای تغییر نام، حذف یا ادغام کامیت‌های قدیمی

۵. ماشین زمان گیت: اصلاح اشتباهات (Undoing Changes)
این بخش همان جایی است که در مواقع بحرانی به داد برنامه‌نویس می‌رسد (دقیقاً مثل تجربه امروز شما).
git checkout -- <file>: لغو تمام تغییرات اعمال شده روی یک فایل و برگرداندن آن به وضعیت آخرین کامیت.
git reset --soft HEAD~1: پاک کردن آخرین کامیت، اما کدهای شما حذف نمی‌شوند بلکه به صف انتظار (git add) برمی‌گردند.
git reset --hard HEAD~1: پاک کردن آخرین کامیت به همراه نابودی تمام کدهای آن. (با احتیاط استفاده شود!).
git revert <commit-id>: ساخت یک کامیت جدید که دقیقاً برعکس کامیت مشخص‌شده عمل می‌کند (بهترین روش برای کدهای پش‌شده به سرور).
git reflog: جعبه سیاه گیت. نمایش تمام جابه‌جایی‌های HEAD روی سیستم شما، حتی اگر کامیت‌ها را هارد-ریست کرده باشید.
git reset --hard <commit-id>: پرتاب کردن کل پروژه به یک کامیت خاص در گذشته (با کمک شناسه‌ای که از reflog یا log می‌گیرید).

۶. ترفندهای حرفه‌ای (Advanced Tools)
git stash,ذخیره موقت و پنهان کردن تغییرات فعلی بدون کامیت کردن (برای زمانی که می‌خواهید سریع برانچ عوض کنید)
git stash pop,بازگرداندن آخرین تغییراتی که به صورت موقت با stash ذخیره کرده بودید
git log --oneline,نمایش تاریخچه کامیت‌ها به صورت خلاصه، زیبا و در یک خط
git cherry-pick <commit-id>,گلچین کردن؛ آوردن یک کامیت خاص از یک برانچ دیگر به برانچ فعلی شما
git rebase -i HEAD~N,شروع عملیات ریبیس تعاملی روی N کامیت آخر برای تغییر نام، حذف یا ادغام کامیت‌های قدیمی
